# 2026-03-13

## Initial repo takeover
- Read `AGENTS.md` and the PM documents in the required order.
- Confirmed the product is a personal navigation assistant built around the service + worker + data loop, not a generic task manager.
- Reviewed `.cursor/plans/phase-0-backend-init_c12c20e8.plan.md` and compared it to the repository state.

## Step 1: backend foundation
- Added project runtime files: `pyproject.toml`, `.env.example`, `.gitignore`, `README.md`, `docker-compose.yml`.
- Added FastAPI app bootstrap, versioned API router, health/readiness endpoints, request ID middleware, and logging setup.
- Added initial tests for config and health.

## Review notes for Step 1
- Fixed readiness settings capture at import time by switching to request-time dependency resolution.
- Fixed request ID cleanup ordering so completion logs still include the request ID.
- Fixed duplicate logging filter attachment on repeated app creation.

## Environment bring-up
- Installed Python 3.12 and project dependencies.
- Configured local `.env` for PostgreSQL and Redis defaults.
- Installed PostgreSQL locally and verified database connectivity.
- Confirmed Docker Desktop and Docker Compose are available locally.
- Fixed config loading so `.env` is resolved from the repository root instead of the ambient working directory.

## Step 2: schema and API skeleton
- Added SQLAlchemy session/base helpers.
- Added core ORM models:
  - `user_state`
  - `state_history`
  - `action_nodes`
  - `node_annotations`
  - `event_logs`
  - `recommendation_records`
  - `recommendation_feedback`
- Added Alembic configuration and the initial migration.
- Applied the initial migration to the local `dln` database.
- Added API schemas and route skeletons for chat, webhooks, state, recommendations, and feedback.
- Added Celery bootstrap and worker task placeholders.

## Review notes for Step 2
- Fixed request-path worker dispatch so chat ingest does not block on Redis availability.
- Kept synchronous ingest limited to validation, event persistence, and ack semantics.

## Step 3: initial persistence wiring
- Connected `GET /api/v1/state` and `POST /api/v1/state/reset` to the real database.
- Added `state_history` writes for manual reset.
- Connected chat and webhook ingest to real `event_logs` persistence.
- Implemented database-backed webhook duplicate handling using uniqueness constraints.
- Connected recommendation pull and feedback APIs to `recommendation_records` and `recommendation_feedback`.

## Step 4: recommendation brief and event processing
- Replaced the placeholder `brief_service` with a database-backed implementation that summarizes active nodes and derives coarse health labels.
- Replaced corrupted placeholder API strings with readable user-facing fallback text.
- Added deterministic event processing:
  - parse `event_logs` into conservative `parsed_impact`
  - apply a state patch to `user_state`
  - write `state_history`
- Connected parse and state worker tasks to the real processing service.

## Review notes for Step 4
- Fixed a data integrity issue where state mutation and state history insertion were split across separate commits.
- Kept event parsing conservative and schema-first within the current deterministic fallback design.

## Verification status
- FastAPI routes have been validated in-process with `TestClient`.
- Database persistence has been validated in-process through SQLAlchemy queries.
- `compileall` passes for the application package.
- `pytest` remains blocked by a machine-specific path permission issue on `E:\Antony\Documents`; this is an environment issue, not yet an application-code failure.

## Current known environment issue
- Python can access files inside the repository, but `pathlib.Path('E:\\Antony\\Documents').stat()` raises `PermissionError [WinError 5]`.
- `pytest` touches that parent path during collection and fails before test execution begins.

## Pytest path workaround
- Created a junction at `C:\Users\Antony\dev\individual-assistant` pointing to `E:\Antony\Documents\individual-assistant`.
- Confirmed `pytest` works when invoked through the junction path.
- Full suite status from the junction path: `9 passed`.
- Practical rule for this machine: keep the repository where it is, but run `pytest` through the junction path until the underlying Windows permission issue on `E:\Antony\Documents` is resolved.

## Step 5: minimum viable recommendation ranking
- Replaced the placeholder pull recommendation service with a database-backed ranking pass.
- Added conservative candidate filtering based on current `user_state` and node energy requirements.
- Added cooldown suppression using `last_recommended_at + cooldown_hours`.
- Added a recent rejection penalty using `last_rejected_at`.
- Added positive scoring for urgency, state fit, confidence, and fresh annotations.
- Recommendation records now persist:
  - filtered candidate IDs
  - selected node IDs
  - ranking snapshot
  - rendered items or fallback payload
- Selected nodes now update `last_recommended_at`.

## Review notes for Step 5
- Kept the route contract stable while replacing the internal ranking implementation.
- Preserved no-candidate fallback behavior rather than forcing low-quality suggestions.
- Fixed the remaining pytest cache warning by adding `pytest.ini` with `cache_dir = .tmp/pytest-cache`.

## Verification update
- Targeted recommendation flow tests passed.
- Full suite status from the junction path is now `10 passed`.

## Step 6: feedback closed loop on nodes
- Replaced the placeholder feedback service with a node-aware implementation.
- Feedback now validates that `node_id` belongs to the recommendation's selected or candidate node set.
- Feedback now updates node-level ranking signals:
  - `accepted` -> set `last_completed_at`, clear `last_rejected_at`
  - `dismissed` / `rejected` -> set `last_rejected_at`
  - `snoozed` -> refresh `last_recommended_at`
- Feedback persistence still lands in `recommendation_feedback` for auditability.

## Review notes for Step 6
- Added an explicit guard against applying feedback to unrelated nodes, so ranking data stays trustworthy.
- Kept the feedback API contract unchanged while making the persistence side effects meaningful.
- Simplified local pytest behavior by disabling the cache provider in `pytest.ini`; the suite now runs cleanly on this machine through the junction path.

## Verification update
- Targeted recommendation flow tests now cover:
  - accepted feedback writing node completion signals
  - rejected feedback writing node penalty signals
  - invalid node feedback returning 400
- Full suite status from the junction path is now `12 passed`.

## Step 7: ranking loop reinforcement
- Added recent completion penalties into the pull recommendation scorer.
- Added same-type recent completion penalties using `drive_type + tags` overlap as the current MVP approximation of “same type”.
- Added exposure fatigue penalties based on recent pull recommendations that exposed a node without accepted feedback.
- Expanded `ranking_snapshot` to persist per-node score breakdowns, not just final scores and reason tags.

## Review notes for Step 7
- Caught and fixed a test-data issue during review where the control candidate accidentally shared the same tag family and therefore correctly inherited the same-type penalty.
- Kept the recommendation API response contract unchanged while making internal ranking reasons more inspectable.
- Preserved fallback semantics and hard filters while adding softer ranking penalties.

## Verification update
- Recommendation flow tests now also cover:
  - recent same-type completion lowering rank
  - exposure fatigue lowering rank
- Full suite status from the junction path is now `14 passed`.

## Step 8: weak push decision path
- Added a dedicated `push_service` to evaluate whether a weak push recommendation should be generated after a state change.
- Push evaluation now:
  - reuses the ranked candidate pipeline
  - checks `do_not_disturb_until`
  - enforces a minimum score threshold
  - suppresses repeated push of the same node inside a short window
  - persists a `recommendation_records` row with `mode='push'`
- Push records now capture either:
  - `delivery_status='generated'` with a selected node and rendered content
  - `delivery_status='skipped'` with an explicit `skip_reason`
- `apply_state_patch` now triggers push evaluation after a successful state update.

## Review notes for Step 8
- Fixed a real worker import cycle by moving push-task import inside the task body instead of wiring it at module import time.
- Caught foreign-key test setup gaps: push recommendation records require a real `event_logs` row for `trigger_event_id`.
- Tightened test cleanup order so recommendation records are removed before their referenced event rows.
- Added defensive test pre-cleanup for push-related fixtures so the suite remains stable even after interrupted local runs.

## Verification update
- Added push evaluation tests for:
  - generated push recommendation
  - do-not-disturb skip behavior
  - state patch task enqueueing push evaluation
- Full suite status from the junction path is now `17 passed`.

## Step 9: parser robustness and state conflict handling
- Split event parsing outcomes into explicit `success`, `fallback`, and `failed` states.
- Parsing now marks:
  - `success` for clear deterministic matches
  - `fallback` for conservative but low-confidence generic interpretation
  - `failed` for events with insufficient usable input
- `apply_state_patch_from_event` now skips state mutation when parsing failed and simply returns the current snapshot.
- Added a compare-and-swap retry test that simulates one optimistic-lock conflict before succeeding.

## Review notes for Step 9
- Kept failed parse behavior conservative: raw events remain persisted, but no synthetic state mutation is applied.
- Verified that the CAS retry path is exercising a real retry branch, not just re-running the happy path.
- Preserved the external API contract while making parse-state semantics more explicit internally.

## Verification update
- Event processing tests now cover:
  - success parse + state patch
  - failed parse with no state mutation
  - CAS retry on version conflict
- Full suite status from the junction path is now `19 passed`.

## Step 10: observability and runtime documentation
- Added reusable structured logging helpers in `app.core.logging`.
- Added structured log fields across the core processing paths:
  - ingest
  - parse
  - state patch
  - pull recommendation
  - push evaluation
- Standardized log payloads around key fields such as:
  - `event_id`
  - `recommendation_id`
  - `user_id`
  - `parse_status`
  - `delivery_status`
  - `skip_reason`
- Updated `README.md` to reflect the current MVP reality:
  - pull recommendations are live
  - feedback learning is live
  - weak push currently only writes decision records and does not send notifications

## Review notes for Step 10
- Kept the logging helper small and string-based so it is easy to reuse without introducing a heavier telemetry dependency.
- Added tests only for helper behavior, not for exact full-log lines in every service, to avoid brittle observability tests.
- Preserved all current service interfaces while improving debugability of the worker path.

## Verification update
- Added logging helper tests.
- Full suite status from the junction path is now `21 passed`.

## Step 11: action node creation and async profiling stub
- Re-checked the PM docs before extending the backend and aligned the next step with the node creation/profiling requirements in the engineering addendum.
- Added a new `POST /api/v1/nodes` endpoint to create action nodes.
- Node creation now:
  - persists immediately
  - applies conservative default profiling values
  - applies lightweight heuristic prefill based on title/tags
  - marks `profiling_status='pending'`
  - attempts to enqueue an async profile task without blocking the main request
- Added a placeholder `profile_new_node` worker so the profiling pipeline is explicitly present in the worker topology.

## Review notes for Step 11
- Kept the synchronous path aligned with the PM: create first, do not wait for profiling.
- Used the PM defaults in the service layer even though the database defaults are not fully identical, avoiding a migration for now while preserving runtime behavior.
- Limited the heuristics to the explicit examples from the addendum rather than inventing a broader classifier.

## Verification update
- Added node creation tests for:
  - conservative defaults
  - heuristic prefill
- Full suite status from the junction path is now `23 passed`.

## Step 12: active node enrichment
- Added a lightweight annotation enrichment service for active nodes.
- `enrich_active_nodes` now refreshes one system annotation per active node with:
  - stable node summary fields
  - a lightweight hint
  - freshness score
  - expiry time
- Added a real enrichment worker task that calls the service and logs enrichment results.
- The current enrichment path does not fetch external data; it only materializes stable internal context into `node_annotations` so downstream freshness logic can work reliably.

## Review notes for Step 12
- Added a defensive re-check before writing annotations so enrichment tolerates nodes disappearing mid-run instead of failing on a foreign-key edge case.
- Fixed test cleanup to rollback first after a failed transaction, preventing local residue from contaminating later ranking tests.
- Kept enrichment intentionally small and internal-only rather than jumping directly to remote data fetches or LLM enrichment.

## Verification update
- Added enrichment tests for:
  - active-only annotation generation
  - replacement of stale system annotations
- Full suite status from the junction path is now `25 passed`.

## Step 13: event log compaction
- Added a lightweight event compaction service for old `event_logs`.
- `compress_event_logs` now:
  - selects old events still in `processed_status='new'`
  - attaches a lightweight `compression_summary` into `parsed_impact`
  - marks those rows as `processed_status='compressed'`
  - preserves the underlying fact records instead of deleting them
- Added a real compression worker task that calls the service and logs compaction results.

## Review notes for Step 13
- Kept compaction reversible enough for future replay by preserving the original row and only adding a summary marker.
- Avoided introducing a separate archive table at this stage, since the current PM requirement is about background maintenance, not yet long-term storage migration.
- Compression logic is intentionally age-based and conservative so it does not interfere with recent-event recommendation behavior.

## Verification update
- Added compaction tests for:
  - old events being marked compressed with summaries
  - recent events remaining untouched
- Full suite status from the junction path is now `27 passed`.

## Step 14: dynamic urgency refresh
- Added a lightweight dynamic score refresh service for active nodes.
- `recalc_dynamic_scores` now refreshes `dynamic_urgency_score` using:
  - deadline proximity
  - stale recommendation bonus for active nodes that have not been touched recently
- Added a real worker task that runs the refresh service and logs its result.

## Review notes for Step 14
- Kept score refresh as a pure recomputation step over existing relational fields, avoiding any new persistence surface.
- Used conservative weighting so the background refresh sharpens ranking without overpowering the direct state-fit and cooldown rules already in place.
- Preserved compatibility with current recommendation logic by updating only the existing `dynamic_urgency_score` field.

## Verification update
- Added dynamic score tests for:
  - near-deadline nodes receiving higher urgency
  - stale untouched nodes receiving a smaller urgency lift
- Full suite status from the junction path is now `29 passed`.

## Step 15: PM-aligned API aliases
- Added a top-level `POST /api/v1/events/ingest` route that reuses the primary chat-ingest path for the single-input MVP.
- Added `GET /api/v1/recommendations/next` as a stable alias of the pull recommendation endpoint.
- Added top-level `GET /api/v1/brief` as a stable alias of the existing brief summary endpoint.
- Updated README scope notes to call out the PM-aligned contract surface explicitly.

## Review notes for Step 15
- Kept all new endpoints as thin aliases over existing services so there is no duplicate business logic to drift.
- Chose route-level alignment rather than introducing a second set of schemas, since the current payload and response shapes already satisfy the MVP contract.
- This closes the remaining gap between the current backend surface and the endpoint names called out in `AGENTS.md` / the PM docs.

## Verification update
- Added API contract tests for:
  - `POST /api/v1/events/ingest`
  - `GET /api/v1/recommendations/next`
  - `GET /api/v1/brief`
- Full suite status from the junction path is now `32 passed`.

## Step 16: async node profile backfill
- Added a dedicated `node_profile_service` so new-node profiling now has a real async backfill path instead of a placeholder task.
- Node profiling now derives a deterministic follow-up profile from stable fields such as:
  - title
  - tags
  - summary
- The async profile worker now updates:
  - energy requirements
  - estimated minutes
  - recommended context tags
  - confidence level
  - profiling status / profiled timestamp
  - lightweight `ai_context` profiling metadata

## Review notes for Step 16
- During verification, the new PM-aligned `/events/ingest` route exposed a real contract gap: duplicate `client_message_id` values were bubbling out as raw database uniqueness errors instead of returning the documented `409`.
- Fixed chat ingest duplicate handling by catching the uniqueness violation, rolling back cleanly, logging the suppression, and returning `409 Conflict`.
- Kept async profiling deterministic and local-only for now, which matches the MVP-first requirement and avoids pulling LLM calls onto a critical path prematurely.

## Verification update
- Added node profile tests for:
  - async profile completion
  - medium-confidence backfill for cognitively heavy work
- Added an API contract test for duplicate `POST /api/v1/events/ingest` returning `409`.
- Full suite status from the junction path is now `34 passed`.

## Step 17: MVP backend status and handoff baseline
- Re-checked the implemented backend surface against the PM addendum and system architecture docs.
- Added a dedicated backend MVP status document summarizing:
  - what is already inside the MVP boundary
  - what is intentionally left for post-MVP
  - what non-MVP engineering gaps are still worth tracking
- Expanded the main README with a short end-to-end verification flow for:
  - ingest
  - state read
  - next recommendation
  - brief summary

## Review notes for Step 17
- Current backend is now at the MVP-complete threshold for the deterministic single-user scope.
- The main remaining engineering gap that still touches PM guidance is webhook idempotency layering: database correctness is in place, but Redis short-term duplicate suppression is not yet implemented.
- This is a good stopping point for backend feature expansion before either:
  - starting frontend integration
  - or beginning post-MVP hardening work

## Step 18: review-driven backend hardening
- Implemented Redis-backed webhook duplicate suppression as the first idempotency layer, while keeping the database uniqueness constraints as the final correctness guard.
- Added top-level webhook occurred-at extraction with deterministic priority:
  - `occurred_at`
  - `event_time`
  - `timestamp`
  - `created_at`
  - `start_time`
  - `updated_at`
- Reworked `reset_state` to use the same compare-and-swap update pattern as event-driven state patches.
- Split candidate filtering and ranking into `app/ranking/candidate_ranker.py` and rewired both pull recommendations and push evaluation to use it.
- Merged request ID extraction into `app/core/request_context.py` and removed the duplicate helper under `app/services/`.
- Removed the dead placeholder `ingest_webhook_event()` entrypoint.

## Review notes for Step 18
- The `docs/mvp_backend_review.md` findings were directionally correct; the highest-value corrections were webhook idempotency layering, webhook occurred-at semantics, and state-reset concurrency alignment.
- Kept the current `+10` energy tolerance intentionally and documented it in the ranking module rather than tightening behavior silently.
- The ranking refactor reduced coupling between `recommendation_service.py` and `push_service.py` without changing the external API contract.

## Verification update
- Added tests for:
  - Redis duplicate suppression and DB fallback behavior on webhook ingest
  - webhook occurred-at parsing from ISO and Unix millisecond timestamps
  - webhook fallback to ingestion time when no payload timestamp is present
  - reset-state CAS retries
  - documented recommendation energy tolerance
  - a full ingest -> parse -> state -> recommendation chain
- Full suite status from the junction path is now `43 passed`.

## Step 19: frontend MVP shell
- Added a standalone React + Vite frontend under `frontend/`.
- Built the MVP integration shell as a single-page app with five areas:
  - state bar
  - local chat timeline
  - command-aware input box
  - recommendation / brief sidebar
  - developer tools panel
- Added a fetch-based API client that mirrors the stable backend contracts and records `X-Request-Id` for local debug visibility.
- Wired the shell to:
  - `GET /api/v1/state`
  - `POST /api/v1/chat/messages`
  - `GET /api/v1/recommendations/next`
  - `GET /api/v1/brief`
  - `POST /api/v1/recommendations/{id}/feedback`
  - `POST /api/v1/state/reset`
  - `POST /api/v1/nodes`
- Added targeted frontend tests for:
  - API client error mapping
  - command routing in the input box
  - recommendation fallback prefill actions
  - developer panel form payload shaping
- Updated repository docs and ignore rules for frontend development.

## Review notes for Step 19
- During review, the first implementation of message retry re-sent text by calling the normal send path, which duplicated the user bubble in the timeline. This was fixed by extracting a shared `submitChatText()` path that can reuse an existing failed entry.
- Moved the frontend source back to ASCII-only UI strings after the initial pass to avoid editor / shell encoding drift and keep the repo consistent with the default editing rule.
- The local Windows junction path (`C:\Users\Antony\dev\individual-assistant`) needed an explicit `preserveSymlinks` Vite setting plus per-test setup imports so Vitest would not drift between the `C:` junction and the real `E:` path during resolution.
- Kept the frontend intentionally thin:
  - no router
  - no websocket / SSE
  - no persisted chat history
  - no webhook simulator UI

## Verification update
- Frontend install / verification completed on the real workspace path:
  - `npm.cmd install`
  - `npm.cmd run test`
  - `npm.cmd run build`
- Frontend status:
  - `4` test files passed
  - `9` frontend tests passed
  - Vite production build passed
- Added `.gitignore` coverage for frontend build artifacts and removed the `tsc -b` byproducts that should not live in the repository root.

## Step 20: frontend shell hardening
- Split the frontend shell orchestration out of `App.tsx` into focused hooks:
  - `useChatFlow`
  - `useRecommendationFlow`
  - `useDevActions`
  - `useDebugEvents`
- Centralized slash-command routing in the app layer so `ChatInput` now submits raw text only and no longer branches on `/pull` or `/brief`.
- Tightened recommendation timeline semantics by separating:
  - `load_failed`
  - `feedback_failed`
  - `feedback_submitting`
- Improved the state bar stale behavior so it now reports stale data both on request failure and when the latest successful state snapshot is older than 60 seconds.
- Expanded frontend debug state from only `lastDebugEvent` to a rolling history of the latest five debug events.
- Added integration-grade `App` tests for:
  - plain chat send -> assistant ack -> state refresh
  - centralized `/pull` handling
  - dismissed recommendation -> repull
  - retrying a failed chat message without duplicating the user bubble
  - dev-panel reset triggering a silent recommendation refresh

## Review notes for Step 20
- The main implementation risk was letting `App.tsx` remain a god component after the MVP shell landed. The refactor keeps the current UI behavior but moves async coordination into hooks so later frontend-backend iteration has clearer extension points.
- During review, the first pass of the new `App` tests used brittle text matchers and fake timers in places where React Query + Testing Library became noisy. Those tests were tightened to assert stable UI outcomes instead of incidental DOM structure, and the query mocks were extended so React Query invalidations do not emit undefined-data warnings.
- Feedback buttons are now explicitly disabled only while feedback is in-flight, which keeps recommendation state transitions clearer during manual integration testing.

## Verification update
- Frontend verification re-run on the real workspace path:
  - `npm.cmd run test`
  - `npm.cmd run build`
- Frontend status after Step 20:
  - `8` test files passed
  - `20` frontend tests passed
  - Vite production build passed

## Step 21: frontend/backend integration kickoff
- Started the first real integration pass against the current MVP shell and stable backend API surface.
- Added a dedicated integration checklist:
  - `docs/frontend-backend-integration-checklist.md`
- Added a categorized issue tracker for live integration findings:
  - `docs/frontend-backend-integration-issues.md`
- Implemented a local ingest fallback for worker-off development:
  - when `ENABLE_WORKER_DISPATCH=false`, chat and webhook HTTP routes now schedule an in-process FastAPI background pipeline instead of stopping at raw event persistence
  - the local pipeline runs parse -> state patch -> weak push evaluation without Redis/Celery
- Added a backend regression test to lock the new local behavior:
  - `POST /api/v1/chat/messages` can now advance state through the route even with worker dispatch disabled
- Updated webhook tests to account for the new background pipeline creating `mode='push'` recommendation records during cleanup.
- Updated README with:
  - the local background-pipeline behavior
  - links to the integration checklist and issue tracker

## Review notes for Step 21
- The first live integration blocker was real: the frontend shell expects eventual state convergence after chat, but the backend previously skipped parse/state work entirely when Celery dispatch was disabled. That made the local shell appear broken even though the UI logic was correct.
- The chosen fix keeps PM constraints intact:
  - synchronous ingest still returns ack quickly
  - heavy work still runs asynchronously
  - local development no longer depends on Redis/Celery just to see state progression
- The follow-up review also exposed a second-order test issue: webhook tests started failing cleanup because the new local pipeline legitimately created push recommendation records referencing the ingested event. Cleanup was tightened instead of weakening the new behavior.
- The remaining live integration blocker is environmental, not contractual: from the Codex execution context on this machine, `npm.cmd run dev` hits `EPERM: lstat 'E:\\Antony\\Documents'`. Frontend tests/build still pass, so this does not block API-level integration or code correctness.

## Verification update
- Backend full suite now passes through the junction path:
  - `44 passed`
- Frontend verification still passes:
  - `npm.cmd run test`
  - `npm.cmd run build`
- Completed a real HTTP smoke pass against a live local uvicorn instance on `127.0.0.1:8010` covering:
  - `/health`
  - `/api/v1/state`
  - `/api/v1/state/reset`
  - `/api/v1/nodes`
  - `/api/v1/chat/messages`
  - `/api/v1/recommendations/next`
  - `/api/v1/recommendations/{id}/feedback`
  - `/api/v1/brief`
- Smoke results confirmed:
  - chat ingest produced a real `event_id`
  - post-chat state moved to `focus_mode=tired`
  - pull recommendation returned a non-empty result
  - feedback recorded successfully
  - brief returned a valid summary payload

## Step 21 follow-up: integration cleanup and proxy verification
- Tightened backend test cleanup for the new worker-off local pipeline behavior.
- `tests/test_webhook_ingestion.py` now snapshots/restores `user_state` and removes `state_history` rows created by webhook-triggered background processing.
- `tests/test_api_contract_shapes.py` now cleans up the `POST /api/v1/events/ingest` contract-shape event plus any derived state/history side effects.
- Re-ran the backend suite from a neutral baseline and verified that `user_state` remains clean after the full test run.
- Completed a second live integration pass through the actual Vite proxy path at `http://127.0.0.1:4173/api/v1/...` covering:
  - `POST /state/reset`
  - `POST /nodes`
  - `POST /chat/messages`
  - `GET /state`
  - `GET /recommendations/next`
  - `POST /recommendations/{id}/feedback`
  - `GET /brief`

## Review notes for Step 21 follow-up
- The remaining dirty-state issue after the first integration pass was not a product bug; it was test pollution caused by API-shape tests and webhook tests preserving event-driven `user_state` mutations.
- Real proxy-based smoke clarified an important runtime behavior: an immediate `GET /state` after chat can still see the pre-event snapshot, but the shell's short-poll reconcile loop converges correctly on the first poll. This matches the PM-approved ack + async processing model and does not require an API change.
- The earlier Vite `EPERM` concern is no longer a blocker for live integration on this machine when the dev server is started through the approved command path.

## Verification update
- Re-ran targeted suites after cleanup fixes:
  - `tests/test_webhook_ingestion.py`
  - `tests/test_api_contract_shapes.py`
- Backend status remains:
  - `44 passed`
- Verified from a clean baseline that a full backend test run no longer leaves `user_state` dirty.
- Proxy-based live smoke confirmed:
  - chat reconcile converges to the new state through polling
  - recommendation pull returns a non-empty item
  - accepted feedback succeeds
  - brief returns a valid summary payload
